precision: 32
max_epochs: 75
num_nodes: 1
sync_batchnorm: True
default_root_dir: ${exp_dir}
num_sanity_val_steps: 0
accumulate_grad_batches: 1
gradient_clip_val: 5.0
# Full training: remove batch limits for complete dataset training
# limit_train_batches: 1  # Disabled for full training
# limit_val_batches: 1    # Disabled for full validation
